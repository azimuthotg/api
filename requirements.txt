# NPU API Project — dependencies
# ติดตั้ง: pip install -r requirements.txt
# (เวอร์ชัน pin ตามที่ใช้งานจริง 2026-06-18)

# Core
Django==5.0.4
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.6.0
django-environ==0.13.0
whitenoise==6.12.0

# Database (MySQL)
mysqlclient==2.2.4

# External integrations
ldap3==2.9.1            # Active Directory auth (NPU.local)
librouteros==3.2.1      # MikroTik RouterOS API
requests==2.32.4        # Home Assistant / Walai / LINE HTTP calls
gspread==6.2.1          # Google Sheets backend (reservapp)
oauth2client==4.1.3     # Google service-account auth (gspread)
google-auth==2.55.0
google-auth-oauthlib==1.4.0
